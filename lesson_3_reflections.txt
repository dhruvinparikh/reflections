When would you want to create a remote repository rather than keeping all your work local?
When a project is handled by more than one person, creating remotes becomes easy for codeintegration.
Why might you want to always pull changes manually rather than having Git automatically stay up-to-date with your remote repository?
To make sure that check points are created and uploaded after logical change.
Describe the differences between forks, clones, and branches. When would you use one instead of another?
forks is used to clone same repository within the github.
Clones are used to create copy of repository locally or from github.
branches are done locally for example to create different version for distribution.
What is the benefit of having a copy of the last known state of the remote stored locally?

to combine conflicting changes form multiple people.
